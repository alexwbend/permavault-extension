name: Retired desktop build

# The fork ships a browser extension. The upstream Electron/Ruffle targets
# were removed; preserve this manual notice instead of running obsolete jobs.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain supported build
        run: echo "Desktop packaging is retired. Use the Build Extension workflow for the browser extension artifact."
